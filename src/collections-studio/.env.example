REPO_ROOT=../..
VAULT_DIR=../../Vault
SAVED_COLLECTIONS_PATH=../../your_instagram_activity/saved/saved_collections.json
COLLECT_COOKIES=firefox

# Must point at the same Postgres instance as ../backoffice/.env's DATABASE_URL —
# reels-pipeline (spawned by lib/job-runner.ts) writes there, this app only reads it.
DATABASE_URL=postgres://backoffice:changeme@localhost:5433/backoffice
